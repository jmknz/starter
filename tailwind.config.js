const colors = require('tailwindcss/colors');

module.exports = {
  purge: ['./src/**/*.html', './src/**/*.js'],
  darkMode: 'media',
  theme: {
    colors,
    extend: {},
  },
  variants: {},
  plugins: [],
};
